import 'package:flutter/material.dart';

class Glessure extends StatelessWidget {
  const Glessure({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Glessure'),
      ),
      body: const Center(
        child: Text('Glessure'),
      ),
      backgroundColor: Colors.white,
    );
  }
}
